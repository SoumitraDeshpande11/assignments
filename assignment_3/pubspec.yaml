name: assignment_3
description: A responsive multi-section dashboard using ListView, GridView, MediaQuery, and Flexible/Expanded.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '^3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
